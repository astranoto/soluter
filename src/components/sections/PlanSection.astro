---
import Icon from "astro-icon";
import PlanCard from "../PlanCard.astro";
import plans from "../../resources/plans.json";
import { twMerge } from "tailwind-merge";

interface Props {
  id?: string;
  containerClasses?: string;
}

const { id, containerClasses } = Astro.props;
---

<section
  id={id}
  class={twMerge(
    "bg-soluter-gray-500 section-padding relative pt-20",
    containerClasses,
  )}
>
  <div>
    <Icon
      name="blocks/kinda-box"
      class="absolute left-4 top-20 hidden h-12 lg:left-8 lg:block 2xl:left-44"
    />
    <h3 class="px-4 text-center font-bold sm:px-8 lg:px-14 xl:px-40 2xl:px-72">
      Il nostro approccio è modulare e personalizzabile in tutte le sue fasi,
      anche quella del pagamento.
    </h3>
    <h4 class="mt-4 px-4 text-center sm:px-6 lg:px-12 xl:px-40 2xl:px-72">
      Ecco perché abbiamo pensato a una formula flessibile e non vincolante, che
      ti permette di valutare la programmazione “step by step”.
    </h4>

    <div
      class="just mt-16 flex flex-col items-center space-y-8 xl:grid xl:grid-cols-3 xl:items-baseline xl:gap-8 xl:space-y-0"
    >
      {
        plans.map((plan) => (
          <PlanCard
            name={plan.name}
            icon={plan.icon}
            iconClasses={plan.iconClasses}
            sections={plan.sections}
          />
        ))
      }
    </div>
  </div>

  <div class="mt-20 flex flex-col items-center">
    <div class="relative">
      <Icon
        name="blocks/crane"
        class="absolute -left-9 h-10 lg:h-14 xl:-left-14 xl:h-20 2xl:left-10"
      />

      <h4 class="px-4 text-center sm:px-10 lg:px-14 xl:px-40 2xl:px-72">
        I nostri SAAS sono sviluppati su misura e lavoriamo al fianco dei nostri
        clienti per costruire esattamente ciò di cui hanno bisogno: per questo
        ci piace, prima di tutto, parlarne per capire insieme quale sia la
        formula più adatta al tuo progetto.
      </h4>
    </div>
    <a
      href="https://calendly.com/astranoto/30min"
      target="_blank"
      class="btn mt-12 bg-soluter-blue-500 px-8 py-1 text-white hover:bg-soluter-blue-500/90"
      >Prenota una call!</a
    >
  </div>
</section>
